# GymNutriAI — Dependências para deploy (Render / API leve)
# Instale com: pip install -r requirements.txt
#
# Para fine-tuning no Colab, use requirements-colab.txt

# === API ===
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
requests>=2.31.0

# === RAG (opcional — pode falhar no Render free por falta de RAM) ===
numpy>=1.24.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2
